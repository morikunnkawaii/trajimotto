<% @posts.each do |post| %>
  <div>
    <%= image_tag post.get_image %>
    <p>投稿画像 <%= image_tag 'sample-author1.jpg' %></p>
    <p>タイトル <%= post.title %></p>
    <p>コメント <%= post.body %></p>
    <p>ユーザー <%= post.user.name %></p>
  </div>
<% end %>